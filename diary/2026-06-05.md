# 2026-06-05 prefix-sharing 开发日志

## 环境搭建

### 本地环境
- 仓库: `~/workspace/project/prefix-proj/prefix-0501_claude-loop/` (claude-loop 分支)
- Python 3.9.6 (macOS), pytest 8.4.2
- 测试结果: **50 passed, 7 skipped** (torch 相关测试因本地无 PyTorch 跳过)
- 跳过的测试: `test_packed_layout`, `test_prefix_store`, `test_torch_reference_backend`, `test_gpu_flash_backend`, `test_verl_megatron_runtime_helpers`

### 远程 GPU 服务器 (219.223.198.62)
- 8x RTX 4090 全部空闲 (0% 利用率)
- GitHub/gh-proxy 均无法访问，通过 rsync 从本地传输仓库
- 磁盘紧张: 仅剩 44GB
- conda 环境: `llm` (PyTorch 2.9.0+cu128)

## 项目现状分析

### 已完成
1. **Core 层** (框架无关): config/detector/planner/store/logprob/batch_trim — 全部实现
2. **Backends 层**: TorchReferenceBackend (完整), GpuFlashAttentionBackend (完整), packed_layout (完整)
3. **Integrations 层**: verl_mcore/megatron_runtime/context/patch_manager — 基本完整
4. **测试**: 52 个测试 (50 passed), 单元/集成/系统三层测试架构

### 待开发 (Roadmap)

#### P0: 精度验证 (最高优先级)
- [ ] 创建端到端精度测试: 对比 prefix-sharing ON/OFF 的 logits/logprobs/loss/gradients
- [ ] 使用 TorchReferenceBackend 在 CPU/GPU 上验证数值一致性
- [ ] 验证不同 prefix 长度 (0, 32, 128, 512, 1024) 的精度
- [ ] 验证 GQA (grouped-query attention) 场景的精度

#### P1: 性能基准与优化
- [ ] 创建性能 benchmark: 测量不同 batch_size/prefix_len 的加速比
- [ ] 对比 TorchReferenceBackend vs GpuFlashAttentionBackend 性能
- [ ] 性能模型: 计算理论加速比并与实测对比
- [ ] 优化 build_kv 中的循环 (考虑并行化)

#### P2: Qwen3.6-27B 模型适配
- [ ] 理解 Qwen3.6-27B 的 HybridAttention 架构
- [ ] 验证 full gated attention 路径 (KV injection + gate)
- [ ] 实现 GatedDeltaNet 状态复用集成
- [ ] 在 GPU 服务器上运行端到端测试

#### P3: 并行策略适配
- [ ] TP=1 基准测试 (单 GPU)
- [ ] TP=2 测试 (双 GPU, 避让使用中的 GPU)
- [ ] TP=4 测试
- [ ] 验证 packed_layout 在不同 TP 下的对齐正确性

#### P4: 代码质量与可维护性
- [ ] 补充 GpuFlashAttentionBackend 的 build_kv/attention 单独测试
- [ ] 补充精度回归测试
- [ ] 代码文档完善
- [ ] 准备独立 Python 包发布

## 关键发现
- `megatron_attention.py` 的 `install()` 方法当前是 stub，实际 patch 通过 `dependency/` 层的 `try/except` import 实现
- `PrefixDeltanetStore` 已准备好框架但未接入真实训练引擎
- Phase 1 约束: 仅 TP, 无 PP/CP, 需要 `use_remove_padding=True`, `use_fused_kernels=False`
