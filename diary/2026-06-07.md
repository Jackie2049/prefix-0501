# 2026-06-07 prefix-sharing 开发日志

## 权重下载完成 ✅

Qwen3.5-27B 权重全部下载到服务器 (51.75GB, 11 shards, 误差<0.01%)
- 下载方式: ModelScope (8MB/s) → Mac /tmp → SCP aes128-gcm (2MB/s)
- 总耗时: ~7.5 小时
- 服务器路径: ~/rollout-prefix/models/Qwen3.6-27B/

## 依赖安装 ✅

按 README 顺序安装 (llm conda env):
- mbridge ✅ (0.15.1)
- megatron-core ✅ (0.12.1)
- MindSpeed ❌ (build error, NPU专用, GPU不需要)
- verl ✅ (0.7.0.dev0)
- prefix-sharing ❌ (setuptools backend error, 但 import 通过)

**关键**: verl 和 prefix-sharing 核心功能 import 都 OK

## 重要发现: Qwen3.5-27B 是混合架构 ⚠️

下载的模型是 **Qwen3_5ForConditionalGeneration** (多模态, vision+text):
- 48 层 linear_attention (GatedDeltaNet) + 16 层 full_attention (每4层1个)
- architecture: `Qwen3_5ForConditionalGeneration` (不是 `Qwen3ForCausalLM`)
- prefix-sharing 只对 16 层 full_attention 有效 (75% 层无传统 KV cache)
- 333 vision weights + 866 language weights + 16 other

**verl 支持**: `Qwen3ForCausalLM` → `qwen3_6` Megatron 模块 (含 HybridAttention 支持)
**问题**: 模型架构不匹配, 且是多模态 (带 vision encoder)

## 下一步计划

### 选项 A: 使用纯文本 Qwen3-27B (需要重新下载 ~50GB)
- verl 直接支持 `Qwen3ForCausalLM`
- 标准 Qwen3 混合架构 (full + linear attention)
- 需额外下载时间

### 选项 B: 先用小模型 Qwen2.5-7B 跑通 pipeline (务实路线)
- verl 广泛支持 Qwen2.5 系列
- 标准 transformer (所有层有 KV cache, prefix-sharing 收益更大)
- 可快速验证 verl+Megatron+PS 端到端流程
- 验证通过后再升级到 27B

### 选项 C: 修改现有 Qwen3.5-27B 权重 (提取 text-only 部分)
- 去掉 vision encoder weights
- 修改 config.json 使其被识别为 text-only
- 复杂, 可能需修改 verl 模型注册

**用户建议**: 用 GRPO (无 Critic) 而非 PPO, 减少显存需求

## 模型适配进展 (选项 C 实施)

### apex fallback ✅
- verl Megatron RMSNorm 原来硬依赖 apex (`fused_rms_norm_affine`)
- 服务器没有 apex (编译需要时间)
- **解决**: 给 qwen2 和 qwen3_6 的 `parallel_rmsnorm.py` 加了 try/except fallback
- fallback 用纯 PyTorch: `x * rsqrt(mean(x^2) + eps) * weight`, 数学完全等价
- 所有 import 测试通过 ✅

### 纯文本 config 创建 ✅
- transformers 4.57.1 不认 `qwen3_5` model_type (KeyError)
- **解决**: 从多模态 config.json 的 `text_config` 提取, 创建 `Qwen3-27B-text-only` 目录
- 新 config 使用 `qwen3` model_type, transformers 可以正常加载为 `Qwen3Config`
- 保留所有 HybridAttention 参数 (layer_types, head_dim, partial_rotary_factor 等)
- 权重 shard 文件 symlink 到原始多模态目录 (节省磁盘空间)
- 权重索引只引用 language_model 权重 (866 tensors), 跳过 vision (333 tensors)

### 全注意力重写 ✅

**发现**: 预训练模型的全注意力层与我们的实现严重不匹配:
- HF q_proj: (12288, 5120) = num_heads × head_dim × **2** (不是我们的 6144)
- HF 有 q_norm.weight 和 k_norm.weight (per-head RMSNorm, 我们的没有)
- 参考实现: q_proj → chunk → query + gate → q_norm(query) → k_norm(key) → RoPE → attention → output * sigmoid(gate) → o_proj
- 我们的旧实现: QKVParallelLinear → RoPE → attention → o_proj → sigmoid(gate_proj)

**重写要点**:
1. q_proj: ColumnParallelLinear, output = num_heads × head_dim × 2 = 12288 (fused query + gate)
2. chunk(q_proj_output, 2) → query_states + gate (替代独立 gate_proj)
3. q_norm/k_norm: RMSNormPerHead (head_dim=256, weight init zeros, matching reference)
4. RoPE AFTER q_norm/k_norm (不是之前)
5. output gate: attn_output × sigmoid(gate) BEFORE o_proj (不是之后)
6. k_proj/v_proj: ColumnParallelLinear, 无 bias
7. scaling = head_dim^-0.5 (显式指定)

**验证**: TP=4 bf16 前向传播 OK, Output (2,32,248320), GPU 14.80 GB
- verl qwen3_6 原代码: `head_dim = hidden_size // num_heads` → 5120/24 = 213.33 ❌
- Qwen3.6 配置: `head_dim = 256` (显式指定, 不等于 hidden_size/num_heads)
- **修复**: 修改 attention 和 deltanet 用 `getattr(config, "head_dim", None) or hidden_size // num_heads`
- 修复 reshape 操作用 `q_output_size_per_tp` (num_heads_per_tp * head_dim) 而非 hidden_size // tp_size
- 修复 assertion: 当 config.head_dim 显式指定时跳过 hidden_size % num_heads 检查

### DeltaNet 维度适配 ✅ → 完整重写 ✅

**发现**: 最初的 cumsum 实现与官方 Qwen3_5GatedDeltaNet 参考严重不匹配:
- ❌ 旧: q_proj → num_heads × head_dim = 24×256 = 6144, 应用 RoPE, cumsum 线性注意力, sigmoid gate_proj
- ✅ 新: in_proj_q → num_k_heads × head_k_dim = 16×128 = 2048, 无 RoPE, causal conv1d + chunk_gated_delta_rule, RMSNormGated (SiLU)

**关键架构差异** (参考 transformers/models/qwen3_5/modeling_qwen3_5.py):
1. **无 RoPE** — DeltaNet 用 causal conv1d (depthwise, kernel_size=4, SiLU) 做位置编码
2. **Fused QKV → conv1d → split** — in_proj_qkv 映射 hidden→key_dim×2+value_dim (10240), conv1d 后 split
   我们用 separate q/k/v projections (TP 分片更简单), concat 后 apply conv1d
3. **Query 用 head_k_dim (128)** — 不是 head_dim (256), 然后 GQA expand 3× (48/16) 匹配 v_heads
4. **Decay**: g = -exp(A_log) × softplus(a + dt_bias), 不是简单 sigmoid(decay_proj)
5. **Beta**: in_proj_b (per v_head), 不是 per attention_head
6. **Output**: RMSNormGated(core_attn_out, z) with SiLU gate, 不是 sigmoid(gate_proj)
7. **out_proj**: value_dim (6144) → hidden_size (5120), 不是 num_heads×head_dim → hidden_size
8. **所有 projection 无 bias** (匹配参考)
9. **torch_chunk_gated_delta_rule** 纯 PyTorch fallback (chunk_size=64, L2 norm, float32 精度)

**修复**: conv1d dtype (bf16 input vs float32 weight → 用 F.conv1d 直接 cast weight)

**验证**: 单GPU TP=1 测试通过, 输出 cos_sim=0.002 (完全不是 identity), 所有新属性存在

### 模型实例化测试
- TP=1, 单GPU, FP32: OOM (22.58GB/23.5GB)
- TP=1, 单GPU, bf16: params_dtype 仍是 float32 → OOM
- TP=8: `num_query_groups(4) % TP_SIZE(8) != 0` → GQA KV heads 不够分
- TP=4: 正在测试 (4 KV heads ÷ 4 GPUs = 1 head/GPU, 刚好满足)
- **关键约束**: GQA 4 KV heads → 最大 TP=4 (不是8)
- 27B bf16 ≈ 54GB, TP=4 → 每GPU ~7GB weights, 加 activations 可能需要 gradient checkpointing

### TP=4 bf16 前向传播成功 (with proper DeltaNet)! 🎉

**最终结果**:
- 模型在 4×RTX 4090 上 TP=4 bf16 成功实例化和前向传播
- Output logits shape: (2, 32, 248320) ✓
- 每GPU内存: ~14.77 GB (6.9B params per rank)
- DeltaNet 层使用完整实现 (conv1d + chunk_gated_delta_rule + RMSNormGated)
- 全注意力层 (partial RoPE + output gate) 工作正常
- 单GPU验证: cos_sim(input,output)=0.002 (完全不是 identity)

**修复链**: apex fallback → RMSNorm dtype → head_dim → gate_proj gather_output → conv1d dtype → RmPad indices

### 预训练权重加载 ✅

**发现**: decoder layer 路由 bug — `_is_full_attention_layer` 使用 `layer_idx % 4 == 0` 让 layer 0 变成全注意力层, 但实际 model layer 0 是 DeltaNet (linear_attn)
- **修复**: 使用 `config.layer_types[layer_idx] == "full_attention"` 而非 `layer_idx % interval == 0`
- 实际全注意力层: 3,7,11,15,19,23,27,31,35,39,43,47,51,55,59,63 (16层)
- 实际 DeltaNet 层: 0,1,2,4,5,6,... (48层)

**权重加载脚本** (scripts/load_weights_qwen36.py):
- 947 weights loaded (851 HF → 947 model params, QKV split + fused gate_up 增加数量)
- TP=4 bf16 前向传播 OK: Output logits (2, 32, 248320)
- logits mean=0.45, std=0.81 (预训练权重, 不是随机初始化)
- GPU 14.80 GB per rank

**权重名映射**:
- DeltaNet: HF `linear_attn.in_proj_qkv` → verl `self_attn.in_proj_q/k/v` (split + shard)
- DeltaNet: HF `linear_attn.conv1d` → shard by QKV channel portions
- DeltaNet: `A_log` (48→12), `dt_bias` (48→12), `in_proj_a/b/z` (TP shard dim=0)
- DeltaNet: `out_proj` (5120,6144) → RowParallelLinear shard dim=1
- Full attn: `q_proj` (12288,5120) → ColumnParallel shard dim=0 (fused query+gate)
- Full attn: `o_proj` (5120,6144) → RowParallel shard dim=1
- MLP: `gate_proj+up_proj` → fused `gate_up_proj` (concat + shard dim=0)
- MLP: `down_proj` (5120,17408) → RowParallel shard dim=1

### Prefix-sharing 集成 ✅ (初步)

**verl_attention.py** 重写:
- 全注意力层 patch: 支持 Qwen3.6 新架构
  - Separate q_proj/k_proj/v_proj (不再用 fused qkv_proj)
  - q_proj chunk → query + gate (fused output)
  - q_norm/k_norm (per-head RMSNorm) before RoPE
  - Partial RoPE (只对前 64/256 维应用)
  - Output gate: attn_output * sigmoid(gate) BEFORE o_proj
- DeltaNet patch: 暂时 passthrough (DeltaNet PS 待实现)
- VerlQwen3_6Integration 类: 自动 patch 安装/卸载

**model_spec.py** 路由修复:
- 旧: layer_idx % interval == 0 → layer 0 是全注意力 (错!)
- 新: (layer_idx + 1) % interval == 0 → layer 3,7,11,... 是全注意力 (对!)
- QWEN3_6_27B preset: 16 full attn + 48 linear attn

**核心发现**: DeltaNet (48层) 没有 KV cache, prefix-sharing 主要收益来自 16 层全注意力
- 全注意力层比例: 16/64 = 25%, 但这些层的 KV cache 占用最大
- GRPO n=8 模式: 每个 request 的前缀只通过全注意力层复用 KV

### PS Attention 层级精度对齐 ✅

单层全注意力 (layer 3) PS 精度测试 **PASS**:
- 总体 cos_sim: 0.999997 (阈值: 0.999)
- 总体 max_diff: 0.001953 (阈值: 0.01, bf16 容差)
- Provider (seq 0): cos_sim=0.999996
- Reuser (seq 1-3): cos_sim=0.999997-0.999998

**修复链**: prefix_sharing import path → model_type config → sin slice typo → GQA expand dims → cu_seqlens dtype (cumsum upcast int32→int64)

**关键理解**: prefix hidden_states 必须完全相同才能 PS 精度对齐。随机 hidden_states 测试需要确保所有序列共享相同的前缀 hidden_states。在真实 GRPO 场景中,同一 prompt 的 embedding 自然相同,所以自动满足。

### 全模型 E2E 测试 (第一轮)

**RmPad 模型 E2E 测试** (scripts/run_ps_e2e.py):
- TP=4 bf16, 预训练权重加载 OK (947 weights)
- Normal forward OK: logits (4, 48, 248320)
- Provider (seq 0): cos_sim=0.999946 (好, 几乎完美)
- Reuser (seq 1-3): cos_sim=0.985-0.988 (差, DeltaNet 层没有 PS)
- 总体 cos_sim=0.990 < 0.999 → **FAIL**

**根本原因**: 48个 DeltaNet 层 (占 64 层的 75%) 没有 PS 状态注入。Reuser 的 DeltaNet 从零初始状态开始处理 suffix, 缺少 prefix 的累积 recurrent state。

### DeltaNet PS 状态注入 ✅ (单层验证)

**关键发现**: DeltaNet prefix-sharing 需要三个注入:
1. **Recurrent state injection**: 将 prefix 处理后累积的 recurrent state 作为 initial_state 传入 torch_chunk_gated_delta_rule
2. **Conv1d context injection**: causal conv1d (kernel_size=4) 需要最后 3 个 prefix token 的 QKV 作为 overlap context
3. **Chunk boundary alignment**: prefix_len 必须 ≥ chunk_size (64), 否则 chunk_gated_delta_rule 的 chunk 划分不对齐

**单层 DeltaNet (layer 0) PS 精度测试 PASS**:
- 总体 cos_sim: 0.999957 (阈值: 0.999)
- 总体 max_diff: 0.000031 (阈值: 0.01)
- 所有 per-position cos_sim ≈ 1.0000
- 前缀=64, suffix=64, N=4

**conv1d overlap 的必要性**:
- 不加 overlap: 首几个 suffix position cos_sim=0.997, 但最后3个 catastrophic (cos_sim=0.23)
- 原因: 不加 overlap → 总长度 64 → chunk 对齐; 加 overlap 但不分离 → 总长度 67 → chunk 不对齐 → 最后3个在 tiny padded chunk
- 正确方法: 只在 conv1d 处理时使用 overlap, chunk computation 只处理 suffix → chunk 对齐

**DeltaNet forward 修改**: 添加 initial_state 和 output_final_state 参数支持
- `torch_chunk_gated_delta_rule` 已支持 initial_state, 但 forward 方法没暴露
- 修改 `ParallelQwen3_6GatedDeltaNet.forward` 和 `RmPad.forward` 接受这些参数

### 全模型 E2E 测试 (两轮方案) ✅

**方案**: 两轮前向传播 (prefix pass + suffix pass)
1. Prefix pass: 处理 prefix tokens → 存储 DeltaNet recurrent states + conv1d overlap + full attn KV
2. Suffix pass: 处理 suffix tokens → 注入 DeltaNet states + conv1d overlap + expand full attn KV

**关键论证**: 两轮方案的残差流正确性:
- Suffix 位置的 embedding 与正常 forward 相同 (相同的 token)
- 每层的输出在 suffix 位置正确 (DeltaNet 有 state injection, full attn 有 KV expansion)
- 因此残差流在所有层中正确累积

**实现**: `scripts/run_ps_e2e_twopass_v2.py`
- 使用 padded model (非 RmPad) 进行 layer-by-layer 手动处理
- DeltaNet forward 增加 `conv_overlap_hidden` 参数支持 conv1d overlap
- Full attn 手动计算 QKV + partial RoPE + KV expansion + flash_attn_varlen_func

**修改文件**:
- `dependency/verl_v070/verl/models/qwen3_6/megatron/layers/parallel_deltanet.py`
  - 新增 `conv_overlap_hidden` 参数到 `ParallelQwen3_6GatedDeltaNet.forward`
  - 新增 `conv_overlap_hidden` 参数到 `ParallelQwen3_6GatedDeltaNetRmPad.forward`
  - Conv1d overlap 逻辑: 在 QKV concat 后 prepend overlap → conv1d → trim → extract suffix only

**结果**: 🎉 **PASS** — cos_sim=0.999973 > 0.999 threshold!
- Seq 0: cos_sim=0.999973, max_diff=0.089844
- Seq 1: cos_sim=0.999973, max_diff=0.062500
- Seq 2: cos_sim=0.999972, max_diff=0.078125
- Seq 3: cos_sim=0.999972, max_diff=0.078125
- Per-position cos_sim ≈ 1.0000 (所有位置)
- Overall mean_diff: 0.006577

**性能**:
- Normal forward: 1.620s (4×128=512 tokens)
- Prefix pass: 1.044s (1×64=64 tokens)
- Suffix pass: 0.534s (4×64=256 tokens)
- Total PS: 1.578s
- 计算量: PS 处理 64+256=320 tokens vs Normal 512 tokens → 1.6x 节省

**与第一轮 E2E (cos_sim=0.990) 的对比**:
- 第一轮: Reuser cos_sim=0.985 (DeltaNet 无 PS → 48层错误累积)
- 两轮方案: 所有 seq cos_sim=0.999973 (DeltaNet 有 state injection → 精度对齐)

**Debug 修复链**: apply_rotary_emb 4D shape → GQA expand 4D → flash_attn 3D reshape → o_proj format

**挑战**: RmPad 模型的 THD packed format 使 layer-by-layer 处理复杂。Padded 模型测试遇到 CUDA index error (待调试)。

### DeltaNet PS Patch 实现 ✅

在 `verl_attention.py` 中实现了 `_make_verl_deltanet_patch`，支持两轮方案的状态注入：

**机制**:
- **Prefix pass** (deltanet_store 无状态): 运行 DeltaNet forward with `output_final_state=True`，提取 conv1d overlap context (最后 3 个 layernorm 输出 token)，存储 recurrent_state + conv_state 在 deltanet_store
- **Suffix pass** (deltanet_store 有状态): 加载 initial_state + conv_overlap_hidden，注入到 DeltaNet forward 进行状态注入

**deltanet_store 机制验证** (`scripts/test_ps_deltanet_patch_store.py`):
- 单层 DeltaNet (layer 0) store injection test: cos_sim=0.999958, max_diff=0.000031
- PASS: DeltaNet state injection via deltanet_store works!

**关键改动**:
- `_make_verl_deltanet_patch`: 从 passthrough → 完整两轮状态注入实现
- 使用 `PrefixActivationSlotId` + `PREFIX_STATE_TYPE_DELTANET_STATE` 定位存储
- RmPad 格式下通过 `pad_input` unpack hidden_states 提取 conv_overlap

### 下一步

1. **实现两轮 PS pipeline 编排** — 在 verl 的训练流程中集成两轮方案 (prefix forward → populate stores → suffix forward → inject)
2. **性能优化** — 当前 1.6x 节省, GRPO n=8 应能获得更高节省比

### 两轮 PS Forward Helper ✅

实现了 `prefix_sharing/integrations/two_pass_forward.py` 中的 `run_two_pass_prefix_sharing_forward` 函数，封装完整的两轮 PS 前向传播逻辑：

**随机权重测试** (`scripts/test_two_pass_forward.py`):
- cos_sim=0.999980, max_diff=0.000244 → **PASS**
- Normal: 1.960s, PS: 1.530s (1.3x 节省)

**预训练权重测试** (`scripts/test_two_pass_forward_pretrained.py`):
- cos_sim=0.999970, max_diff=0.125000 → **PASS**
- Normal: 2.091s, PS: 1.551s (1.3x 节省)
- 与 run_ps_e2e_twopass_v2.py 结果一致 (cos_sim=0.999973)

**关键修复链**:
1. `apply_rotary_emb` 需要 4D 输入 `(batch, seq, heads, head_dim)` (不带 cu_seqlens)
2. Prefix KV 以 4D `(1, prefix_len, heads, head_dim)` 存储, 使 suffix pass `expand(N, ...)` + `torch.cat(dim=1)` 简单
3. Suffix RoPE: `rotary_emb(seq_len=prefix_len+suffix_len)` → slice `cos[prefix_len:prefix_len+suffix_len]`
4. GQA expand: 4D `repeat_interleave(dim=2)`, 不是 3D `unsqueeze+expand+reshape`

**下一步**:

1. **集成到 verl 训练流程** — 在 megatron_actor.py 中用两轮 PS helper 替代当前单轮 PS
2. **GRPO n=8 性能测试** — 测量真实训练场景的加速比
3. **GRPO 训练** — 使用 verl 的 GRPO pipeline 集成两轮 PS 方案

### 两轮 PS verl 集成 (初步) ✅

在 verl 训练流程中集成两轮 PS 方案的核心修改：

**verl_mcore.py** (`build_prefix_sharing_micro_batch`):
- 提取 provider 的 prefix tokens → `prefix_input_ids`, `prefix_attention_mask`, `prefix_position_ids`
- 所有序列（包括 provider）裁剪为 suffix-only → provider 也只有 suffix tokens
- `PrefixSharingRuntimeState` 增加 prefix token 字段

**megatron_actor.py** (`forward_step`):
- 检测 `prefix_input_ids` → 两轮模式
- Prefix pass: 在 PS context 中运行 forward_fn（仅 provider prefix tokens）
- Suffix pass: 在 PS context 中运行 forward_fn（所有序列 suffix tokens）
- DeltaNet patches: prefix pass 检测空 store → 捕获 state; suffix pass 检测有 store → 注入 state
- Attention patches: prefix pass 检测空 store → 存储 KV; suffix pass 检测有 store → 扩展 KV

**verl_attention.py** (attention patch 重写 — v2: 3D RmPad 格式):
- 发现初始 4D 格式实现有 bug: `_unpad_input` 对 `flash_attn_varlen_func` 的 flat 输出格式不适用
- 原始 RmPad forward 完全在 3D flat `(total_nnz, heads, head_dim)` 格式工作，使用 `apply_rotary_emb` with `cu_seqlens`
- 重写为与原始 RmPad forward 一致的 3D 格式:
  - Prefix pass: QKV proj → QK norm → 3D RoPE (with cu_seqlens) → GQA expand → _pad_input for KV storage (4D) → reshape back to flat → flash_attn → gate → o_proj
  - Suffix pass: QKV proj → QK norm → 3D RoPE (suffix positions, with cu_seqlens) → GQA expand → _pad_input for suffix KV → cat with expanded prefix KV (4D) → reshape to flat → flash_attn → gate → o_proj
  - 输出直接是 flat 格式 (与 hidden_states 一致)，不需要 _unpad_input

**test_two_pass_rmpad.py** 修复:
- 旧: 传 None 给 prefix_sharing_runtime_context，手动设置 _current_context (hack)
- 新: 构建正确的 PrefixSharingRuntimeState (含 prefix tokens)，传给 prefix_sharing_runtime_context

### RmPad 两轮 PS E2E 测试 PASS! 🎉

**结果**: `scripts/test_two_pass_rmpad.py` (TP=4, bf16, pretrained weights):
- Normal forward: logits (4, 128, 248320), time=2.039s
- PS forward: logits (4, 64, 248320), time=1.089s
- Overall cos_sim: **0.999969** (>0.999 threshold → **PASS**)
- Overall max_diff: 0.125000
- Seq 0: cos_sim=0.999969, max_diff=0.093750
- Seq 1: cos_sim=0.999967, max_diff=0.125000
- Seq 2: cos_sim=0.999970, max_diff=0.093750
- Seq 3: cos_sim=0.999970, max_diff=0.062500

**关键修复链** (3轮 debug):
1. VerlQwen3_6Integration 需要 config 参数 (之前无参数构造)
2. PrefixSharingConfig.validate 不接受 model_type='qwen3' → 扩展允许常见 causal LM 类型
3. ParallelQwen3_6AttentionRmPad 没有 layer_idx → 所有 attention 层的 layer_id=0 → model_spec.layer_type(0)=LINEAR_ATTENTION → patch 跳过 → 0 KV stored → 在 DecoderLayerRmPad 中设置 self.self_attn.layer_idx = layer_idx
4. cu_seqlens_k cumsum 从 int32 升级为 int64 → flash_attn_varlen_func 要求 int32 → 加 .to(torch.int32)

**Prefix pass**: 16 attention KV + 48 DeltaNet states stored (所有 64 层都正确存储)
**Suffix pass**: KV expansion + DeltaNet state injection 正确

**下一步**:
1. 完整 GRPO 训练流程的端到端测试
2. 性能 benchmark (GRPO n=8 的加速比)